steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/aisa-frontend:$COMMIT_SHA'
      - '--build-arg'
      - 'VITE_GOOGLE_CLIENT_ID=743928421487-tgh59ajhsmuk5ltomsooj46lials3hpt.apps.googleusercontent.com'
      - '--build-arg'
      - 'NEXT_PUBLIC_API_URL=https://efvbackend-743928421487.asia-south1.run.app'
      - '.'

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/aisa-frontend:$COMMIT_SHA']

  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'aisa-frontend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/aisa-frontend:$COMMIT_SHA'
      - '--region'
      - 'asia-south1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'NEXT_PUBLIC_API_URL=https://efvbackend-743928421487.asia-south1.run.app'

images:
  - 'gcr.io/$PROJECT_ID/aisa-frontend:$COMMIT_SHA'